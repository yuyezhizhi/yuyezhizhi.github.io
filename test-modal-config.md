# 文件预览页配置文件弹窗功能测试

## 需求说明
用户希望文件预览页默认展示文件列表，而修改配置文件的功能用弹窗形式展示。

## 修改内容

### 1. 默认展示文件列表
- 将 `activeTab` 初始值从 `'config'` 改为 `'files'`
- 移除了原有的Tab切换功能，直接展示文件列表

### 2. 添加配置文件弹窗
- 在 `data()` 中添加 `showConfigModal` 状态变量控制弹窗显示
- 创建了完整的弹窗结构：
  - `modal-overlay`：半透明遮罩层
  - `modal-container`：弹窗容器
  - `modal-header`：弹窗头部（标题+关闭按钮）
  - `modal-content`：弹窗内容（包含原配置文件管理功能）

### 3. 添加打开弹窗的按钮
- 在文件列表头部添加了"修改配置文件"按钮
- 按钮使用设置图标和蓝色背景，与刷新按钮风格一致

### 4. 弹窗功能
- 点击遮罩层空白区域可关闭弹窗
- 点击关闭按钮可关闭弹窗
- 弹窗内保留了完整的配置文件管理功能：
  - 文件上传
  - 配置文件预览
  - 下载配置文件
  - 使用说明

### 5. 样式优化
- 弹窗使用现代化设计，圆角边框和阴影效果
- 按钮有悬停效果
- 添加了响应式设计，在移动端优化显示

## 测试方法

### 1. 默认展示验证
- 访问 `http://localhost:5174/file`
- 预期结果：直接显示文件列表，不显示配置文件管理界面

### 2. 弹窗功能验证
- 点击文件列表头部的"修改配置文件"按钮
- 预期结果：弹窗弹出，显示完整的配置文件管理功能

### 3. 文件上传功能
- 在弹窗中点击"选择文件"按钮，选择一些MD或PDF文件
- 预期结果：文件添加成功，配置内容更新

### 4. 下载配置文件
- 在弹窗中点击"下载配置文件"按钮
- 预期结果：配置文件下载成功

### 5. 弹窗关闭
- 点击弹窗右上角的关闭按钮
- 点击遮罩层空白区域
- 预期结果：弹窗关闭，返回文件列表界面

### 6. 响应式测试
- 在移动端设备或浏览器开发者工具中测试
- 预期结果：弹窗和按钮布局适应小屏幕

## 修改文件路径
- `src/views/FilePreview.vue`